/**
 * Admin view of Emergencias — renders inside the admin layout/sidebar.
 * The actual page content lives in /emergencias/page.tsx and is re-exported here
 * so the admin never leaves their layout when navigating to this module.
 */
export { default } from "@/app/emergencias/page";
