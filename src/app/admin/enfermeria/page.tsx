/**
 * Admin view of Enfermería — renders inside the admin layout/sidebar.
 * The actual page content lives in /enfermeria/page.tsx and is re-exported here
 * so the admin never leaves their layout when navigating to this module.
 */
export { default } from "@/app/enfermeria/page";
